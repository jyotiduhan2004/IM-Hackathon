# Cycle 10 smoke — validator + live trace summary

**Run**: `compile_run_id=da82773f-019a-43de-b297-98f64c2817f6` (`--limit 10 --batch-size 1`)
**Date**: 2026-04-18T14:23-14:43 IST
**Cost**: $0.50 (remaining budget $2.15)
**origin/main**: `eb658d8` (all 10 v10 PRs + review follow-on #197 merged)

## Outcome

| Batch | Thread | Model | Turns | Outcome | Notes |
|-------|--------|-------|-------|---------|-------|
| 1 | 19bb55f3f80f | grok-4.1-fast | 5 | trivial_skip | Reply agrees to meeting |
| 2 | 19bb66cef7aa | grok-4.1-fast | 18 | **compiled 1 page** | |
| 3 | 19bb66cef7aa | grok-4.1-fast | 5 | trivial_skip | 3 bugs reported |
| 4 | 19bb66cef7aa | grok-4.1-fast | 3 | **kept pending** | Self-healing correctly |
| 5 | 19bb72fc7488 | glm-5 + grok | 18 | **compiled 2 pages + merge_candidates+=2** | U14 handoff fired live |
| 6 | 19b59cdc863a | grok-4.1-fast | 6 | trivial_skip | Varnish scaling announcement |
| 7 | 19bbc09f5bf3 | glm-5 + grok | 22 | **compiled 1 page** | 92% cache |
| 8 | 19b9757e1fcd | grok-4.1-fast | 5 | trivial_skip | Short impact request |
| 9 | 19bbc0e7d4af | grok-4.1-fast | 6 | anomaly (see below) | IM-generated brochure launch |
| 10 | 19bbc1027a19 | glm-5 | 12 | trivial_skip | Repositioning announcement |

**Totals**: 4 pages normalized (via batches 2+5+7), 5 trivial_skips, 1 kept_pending, 1 anomaly, 0 failed.

## v10 features validated live

- **v9-U1 prompt alignment (prompts.py)** — pages normalized on batches 2/5/7 all contain required 8 H2s + `domain:` + lead paragraph. Schema teaching is holding.
- **v9-U14 merge-candidate handoff** — fired on batch 5 (`seller-bl-api-hit-optimisation` + `seller-bl-user-details-verification-api-optimization` flagged as dupes). `wiki/merge_candidates.md` got 2 entries with the canonical apply-script recipe.
- **v9-U2 scorecard math** — not re-validated here; trust the tests.
- **v9-U11 resolve_page recency** — `top_tools=resolve_page:N` in every batch confirms it's the primary discovery tool; didn't re-tier this run.
- **v10-U5 glob narrowing** — `glob` still appears in `top_tools` summaries for some batches (e.g., batch 2 shows `glob:5`). Middleware is narrowing `**/<slug>.md`, not retiring all glob — expected behaviour. Need Langfuse per-obs scores (#185 from v9-U13) to confirm timeout rate drop over 24h.
- **v10-U6 structured responses** — default concise on `list_wiki_pages` / `get_page_summary` / `get_thread_context` — token budget per batch ranged 62k-540k (thread 5 had the big one, dominated by page writes not tool responses).
- **v10-U14 model rotation** — 3 batches used both glm-5 + grok-4.1-fast (coordinator retried after one failed empty response), 7 stayed on their initial pick. 0 failures counted.

## Anomalies + open questions

1. **Batch 9 counted as `compiled` but `normalized 0, touches_inserted 0`.** (`19bbc0e7d4afdd28`, launch announcement of IM-generated brochure.) Per CLAUDE.md rule, `compile_state` should flip only when raw_path is cited in a content-type page. `catalog_synced 6` suggests other pages were synced but no touch row was written for this email. Either:
    - the coordinator still flipped the email to `compiled` despite zero touches (bug), OR
    - the status column in `wiki/log.md` records "compiled" for anything that wasn't trivial_skip/pending — i.e. log-schema shows coordinator behaviour ambiguously, not a real bug.
    Pull the DB state of `19bbc0e7d4afdd28` to resolve. If the email is actually `compiled` in the DB with no `message_touched_pages` row, this is a regression against the U1-era guardrail.

2. **Glob still being called** — batch 2 shows `glob:5`, batch 5 shows `glob` absent, batch 9 missing the field. The v10-U5 narrowing middleware targets `**/<slug>.md`; legitimate `wiki/topics/*.md` patterns still pass through. Next check: per-obs `glob_latency_ms` + `glob_timed_out` scores from #185 over the next 24h — if they drop meaningfully, we're fine.

3. **Cache hit rate is bimodal** — cold start batches (0.7%-43%) vs warm (78%-96%). This is the deepagents / anthropic cache TTL dance (5 min). Known; nothing actionable here since the smoke ran in tight succession but earlier pauses blew cache repeatedly.

4. **`wiki/entities/index.md` still in this checkout** — v10-U1 migration ran against the worker's worktree only (wiki/ is gitignored per-worktree). The main checkout and other long-lived worktrees still carry the legacy page. Not a v10 regression; file under "migration only flows through DB reliably, wiki/ state is worktree-local".

5. **mdlint backlog** — validator flags MD024 (duplicate-heading), MD009 (trailing-spaces), MD047 (no-trailing-newline), MD026 (trailing-punctuation) on ~20 pages. The post-batch pymarkdown autofix hook doesn't catch MD024 (semantic issue). Non-blocking.

## Next wave — v11 candidates

Sourced from **Langfuse user comments 2026-04-18** on trace `6a44c28cb8399e97cf03c40acc45f96e` (thread `19b49539ba25592d`, glm-5, `bl-purchase-whatsapp-9696`), validated against 521 compile traces over 7 days:

| Unit | Title | Prevalence | Risk |
|------|-------|------------|------|
| V11-U1 | Strip `mark_as_compiled` coordinator leak from `scripts/compile_all.py` batch prompt | **100%** (521/521 traces) | low — 1-line delete |
| V11-U4 | Rank `resolve_page` candidates by pg_trgm + source_count + last_compiled (supersedes v9-U11 touch-count tiebreaker) | 56% of resolve calls (633/1128) | medium — needs pg_trgm install |
| V11-U3 | Wrap `read_file` with truncation hint: `total_lines=N, next offset=X` | 83% of traces use read_file | medium — replaces deepagents' inherited tool |
| V11-U5 | TL;DR field in `get_page_summary` + validator-enforced `## TL;DR` section | **64.5%** of gps calls re-read same slug (0.9% act on summary alone) | low |
| V11-U2 | Concise `get_thread_context` returns `raw_path` / `raw_suffix`, drops `one_line`, annotates `cutoff_date` | **96.8%** of concise calls immediately re-read | low |
| V11-U6 | Rename `check_my_work.file_path` → `raw_email_path`; document semantics; wire per-email TodoWrite discipline | 100% use raw path (912/912 calls); 20.5% return `blocked` | low (rename) / medium (behavior) |

**V11-U1 note**: v9-U1 removed "coordinator" from `prompts.py`. The SAME vocabulary leak lives in `scripts/compile_all.py`'s per-batch prompt template at L1660-1668 — the agent sees "Do NOT call any tool to mark these emails compiled — the coordinator handles that deterministically after you return" every batch. Delete the line; keep "Focus on writing the right wiki content."

## Verdict

Cycle 10 smoke is green for shipping. The v10 wave is working as designed — citation guardrails hold, U14 handoff fires, schema-aligned pages emerge. The next bottleneck is **agent workflow efficiency** (concise payloads that don't close the loop, resolve_page ranking, read_file truncation) rather than correctness. V11 is a prompt + tool-UX round, not a correctness round.

Budget runway: $2.15 after smoke. A 5× scale (`--limit 50`) would cost ~$2.50 and risk busting the budget; wait for top-up (#114) before scaling further.
