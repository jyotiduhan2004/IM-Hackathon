# Wiki Quality Trend — 2026-04-12T21:41Z

Comparing Audit 1 (`audit-20260412T200427Z.md`, 22 raw emails → 68 pages) vs
Audit 2 (`audit-pass2-20260412T211108Z.md`, ~200 pages) vs current state
(263 pages: 49 topics / 188 entities / 26 systems).

## 1. Fixes that landed and are confirmed working

- **`export-indiamart` / `tawk-to` byte-duplication resolved.** `export-indiamart-com.md` is now a correct one-line pointer to `export-csl-tracking`; `tawk-to.md` stands alone. CHANGELOG `ae5f0e1` added `check_duplicate_bodies` lint.
- **`page_type` re-categorisation.** `amarinder-s-dhaliwal` correctly in `entities/`; `wazuh-mcp` correctly in `systems/`. Backed by `check_page_type_mismatch` lint.
- **`pratik-ahuja` stub backfilled** (0 → 3 sources by pass 2; still under actual grep count, but no longer empty).
- **Compile pipeline hardened.** Frontmatter parser now tokenises `---` only on own line (CHANGELOG `49f2741`); `update_wiki_index` refuses to auto-stamp mid-broken pages; `scripts/validate_wiki.py` + pre-compile snapshotting landed (`01b8e50`).
- **Title-Case wikilink regression fixed** (`235dc74`, `b95f7da`) — canonical kebab enforced.

## 2. Issues that persist (audit 1 → audit 2 → today)

- **Orphan stubs still empty.** `lucky-agarwal` and `wazuh-mcp` remain the exact "Stub page auto-created because X was referenced" scaffold with `sources: []`. First-audit recommendation #1 (second-pass stub-filler) still unimplemented.
- **Bug/ticket matrix amnesia.** Dropped in audit 1 (`ios-performance-fix`, `export-pii-masking`, `live-assistant`), still dropped in audit 2 (`dynamic-smart-rfq-form` 12-bug matrix, `auditmate` ticket 646247 + 4 open bugs). Current `auditmate.md` and `dynamic-smart-rfq-form.md` confirm — still no `## Bugs / Tickets` section.
- **Entity role/title extraction never shipped.** Audit 1 flagged Kajal, Rohin; audit 2 flagged Saurabh, Vinay, Pratik Singh, Sandeep Garg. Current `sandeep-garg.md` and `mohak-saxena.md` still have no `Role:` line scraped from signatures.
- **Source-file undercounting.** `sandeep-garg` cited in 1 raw file but present in 54 (audit 2); unchanged today. Habitual CC mentions never get linked.
- **Trailing-block duplication bug.** Audit 1: `neeraj-agrawal.md`. Audit 2: `crashagent.md`. Current `mohak-saxena.md` EOF shows `]] - Central AI tools hub` orphan truncation — same chunker class of bug, new manifestation.

## 3. New issues that emerged (not in audit 1)

- **Dead wikilink `[[buylead]]`** in `ab-testing-buylead-search-autosuggest-homepage-ui.md` — no `systems/buylead.md` exists. Net new.
- **`sandeep-kaur1.md` slug-disambiguation ugliness** — email-prefix suffix leaked into slug. Cosmetic but new.
- **Technical-depth loss expanding.** Audit 1 flagged it on 4 pages; audit 2 shows `trustpulse.md` captures only ~25% of source (drops pricing, worked example, 8-tab drawer, test creds, sprint context). Current file confirms — no change.
- **Incomplete-frontmatter errors resurfacing.** `lint_wiki.py` now reports 2 pages missing `last_compiled` (`pns-number-display-m-site-pdp.md`, `rucha-patil.md`) — the exact failure mode CHANGELOG claimed was fixed by the `_split_frontmatter` rewrite.
- **Overstated attribution.** `mohak-saxena.md` claims "oversight for platform-level blocker rollout" on OP-635162 where source only says "for the guidance" (audit 2). New class of soft-fabrication.

## 4. Quantitative trend

| Metric                      | Audit 1 (68 pgs) | Audit 2 (218 pgs) | Now (263 pgs) |
| --------------------------- | ---------------- | ----------------- | ------------- |
| Stub pages (`"stub"` tag)   | 4 known          | 3 known           | **2** (`lucky-agarwal`, `wazuh-mcp`) |
| Duplicate bodies            | 1 (`tawk-to`)    | 0                 | **0** |
| `page_type` mismatches      | 2                | 0                 | **0** |
| Dead wikilinks              | 1 (`export-indiamart`) | 1 (`buylead`) | **17 broken-link warns** across 9 files |
| Orphan entity pages         | not measured     | not measured      | **7** |
| Incomplete frontmatter      | 0 reported       | 0 reported        | **2 ERRORs** |
| Pages with bug-matrix loss  | 4 sampled        | 2 sampled         | systemic (unchanged) |

**Verdict**: structural bugs (dupes, misclassification) trending **better**; link-graph health trending **worse** as the wiki grows (17 dead links up from 1–2); content-depth losses **unchanged**. Growth (+195 pages) is outpacing backfill quality.

## 5. Top 3 quality investments for tomorrow (ROI-ordered)

1. **Ship the second-pass stub-filler.** Same recommendation every audit. Enumerate pages with `sources: []` or `last_compiled == "stub"`, reverse-grep raw files for the slug / name variants, recompile each with those raws pinned. Success gate: 0 stubs at commit. Fixes 2 current stubs + prevents all future ones. ~2 hours; kills an audit-recurring defect class.
2. **Emit bug/ticket matrices verbatim.** Detect markdown tables with header cells matching `Bug ID|Ticket|Work Package|OP-\d+` in source; write them as-is under `## Bugs / Tickets`. Stop prose-summarising. Recovers the single highest-value content class dropped (IDs, ticket counts, pass/fail splits) — what engineering actually searches for. ~2 hours.
3. **Promote `lint_wiki.py` broken-link warnings to build-failing errors.** Today 17 broken wikilinks are `WARN`, not `FAIL`. Wire into `compile_all.py` post-validator so broken links block commit (with an allowlist escape hatch for known-future pages). Caps link-rot before it compounds past 200 links. ~30 minutes.

Secondary (defer): signature role extraction, source-file completeness pass. Both are audit-recurring but lower immediate user impact than the three above.
