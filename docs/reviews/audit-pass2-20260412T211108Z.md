# Wiki Audit — Pass 2 (2026-04-12T21:11Z)

## Summary

Sampled 14 pages (6 topics, 6 entities, 2 systems) out of 218 (38 topics / 159 entities / 21 systems) against 501 raw emails. Most pages from this evening's compile are factually clean (no fabricated numbers/IDs in the prose sampled), but the compiler still **drops ticket IDs and bug matrices wholesale** and **mis-links sources for people referenced in many threads**. Notable delta: the first-audit `export-indiamart`/`tawk-to` byte-duplication bug is **fixed**, and the two `page_type` errors (`amarinder-s-dhaliwal`, `wazuh-mcp`) are **correctly re-categorised** — but both now exist as empty stubs, so the fix only moved the problem.

Sampled: 14 · Clean prose: 13 · Structural errors: 4 · Content gaps: 9.

## Per-page findings

### Topics

**`crashagent.md`** — All claims (Claude Code CLI, systemd 09:00 IST, GITLAB_TOKEN, guardrails, leadership Qs) trace to `ada5fabf`. **Minor gap**: thread contains richer detail (Crashlytics MCP, GitLab API, pipeline report w/ MR URL & log-file path, weekly-report plan) that was elided. **Structural**: duplicated Team/Related blocks at EOF (lines 87-103) — same chunking bug I flagged for `neeraj-agrawal.md` last audit.

**`grafana-mcp-server-integration.md`** — Verified against `c3195b27`: endpoints (10.233.0.65 / 34.14.201.13), LiteLLM routing, K8s Secrets, rate-limit-per-day, K8s MCP next. Accurate and complete.

**`dynamic-smart-rfq-form.md`** — Overview facts (OFR IDs 139658893213, 139632695046, ISQ lists, Dinesh's "open to select") verify against `107e2b59` + `fb8b35ba`. **Gap**: the February pre-launch Bug Matrix in the thread (12 bugs 641797, 641976, 641939, 641947, 642002, 641874, 641892, 641861, 641980, 642106, 641883, 642091) is entirely absent from wiki. Also missing: 80% AI-ISQ relevancy rate from Nishu's Feb-26 analysis (large product-table data).

**`trustpulse.md`** — Weekend build, Bolt.new + Claude + MongoDB Atlas, planned P1/P2/P3 items, AD's approval verify against `119ab12a`/`1f026ebf`. **Gap**: page drops the Pi Day sprint context (March 14, 2026, 24h unlimited Claude tokens), 62 active cases count, Welcome Call Funnel 14→13 (93% conversion), ₹999/₹9,999 pricing, 8-tab drawer (Overview/Stages/Timeline/AI Brief/Messages/Call Script/Notes/Renewal), Balaji GLID 44797594 worked-example, test URL & test creds, and the planned Curated Seller API integration. This is a richly-documented source; wiki captures ~25% of it.

**`ab-testing-buylead-search-autosuggest-homepage-ui.md`** — Test coverage (iPhone 11/iOS 26.3.1, vivo Y16/Android 12, GLIDs 10359580 + 247455444), test counts (21/20/1, 20/19/1, 23/22/1), bug IDs (655257, 655246, 655245), 14 functional scenarios all verifiable in `14cfa7b0`. **Minor gap**: the four negative scenarios (dup keywords, long-keyword wrap, empty categories, free-seller gating) were dropped. `[[buylead]]` wikilink in frontmatter **does not resolve** — no `systems/buylead.md` exists.

**`m-site-home-page-html-rewrite.md`** — All claims (39% reduction, 644634, 644626, GLIDs 59063812/234210977/258373918, login modes, browsers, countries, section-pass list, Phase 2 plan) verify against `284316a8`. Clean.

**`llm-based-top-search-seller-thank-you-page.md`** (bonus, spot-checked) — GPT 4.1 mini → 5.4 mini swap date (Apr 3), 60% p50, 85%→10% timeouts, 1.6%→4.3% mobile CTR, Amit's GPT OSS 120B / Cerebras suggestion, DA's Grok 4.20 — all verified against `f6082d8c`/`e6c6638f`.

### Entities

**`mohak-saxena.md`** — Intent-Based GST Blocker claim: wiki attributes "platform-level blocker rollout" and Work Package OP-635162. Source (`0c1d01fc`) attributes OP-635162 to the *project*; Mohak's named role in that thread is narrower — "for the guidance." Wiki's "oversight for platform-level blocker rollout" is a reasonable inference but **overstates attribution**. Everything else verifies.

**`saurabh-sharma.md`** — Email `saurabh.sharma4@indiamart.com` verified. Role missing: signature in `2bd5c30d` is "Engineer - Marketplace" — page has no `Role:` block. Claims accurate.

**`vinay-kaushal.md`** — Email verified. Claims (comparison PPT assignment by EOD) match Anshu's ask in `61347281`. Accurate but thin; no `Role:` block.

**`pratik-singh.md`** — Accurate but title missing. Source signature: "Sr. Engineer (iOS)" — page says only "Team member responsible for CrashAgent implementation."

**`sandeep-garg.md`** — Wiki links him to `ios-performance-fix-login-flow-v13-6-6` via the `3c5acaba` source, but grep shows `Sandeep Garg` appears in **54** raw files. The single source captured here misses that he's a cross-cutting CC/stakeholder. No fabrication; severe source-completeness gap.

**`paras-lehana.md`** — "Co-presenter" claim verifies against `1357413e` ("Presenters: Abhinav Kaushik, Paras Lehana"). "Working extensively on Windmill POC" reasonably inferred (Abhinav wrote "We shall be working extensively on this during our POC" from a thread where Paras is listed as co-presenter).

**`amarinder-s-dhaliwal.md`** — **Correctly moved to `entities/`** (fix from first audit). But `sources: []` and body is an auto-generated stub. AD appears in hundreds of threads and is the approver in `1f026ebf` (TrustPulse budget) among many others. Compiler's entity-backfill step is not running on referenced-but-unseeded entities.

**`lucky-agarwal.md`** — Still an empty stub (flagged in first audit too). No progress.

### Systems

**`auditmate.md`** — Matches `910283f9` for Seller Journey + Gladmin RCA-screen flow + date-time picker. **Gap**: wiki omits Ticket 646247, identified bug 655023, "Open Bugs: 4 (H:0 / M:0 / L:4)", 7-day history window, and "Way Forward" list (UI/UX, re-enable Title/Price editing, Category Popup update).

**`gladmin.md`** — URL `https://gladmin.intermesh.net` verifies. Agent Hub description and Design Guideline Agent references correct. Accurate but shallow.

**`export-indiamart-com.md`** — **Previous audit's bug fixed** — no longer a clone of `tawk-to.md`; it's now a proper one-line page pointing at `export-csl-tracking`.

**`wazuh-mcp.md`** — **Correctly moved to `systems/`** (fix from first audit). But still an empty stub with `sources: []` — its source (the wazuh MCP integration thread) was not re-linked.

## Cross-wiki findings

1. **Stubs not backfilled**: `amarinder-s-dhaliwal`, `wazuh-mcp`, `lucky-agarwal` — all referenced from real topic pages, all have `sources: []`. The compiler's stub-creation step (`"was referenced but no page existed"`) isn't followed by a second pass that ingests the raw files those references sit in. **Same failure mode as first audit — not fixed.**
2. **Dead wikilink**: `topics/ab-testing-buylead-search-autosuggest-homepage-ui.md` links `[[buylead]]` which has no file.
3. **Bug-matrix amnesia (systematic)**: both `dynamic-smart-rfq-form.md` (12 bugs) and `auditmate.md` (4 open bugs + ticket 646247) lose the bug/ticket lists. Same pattern as first-audit (`ios-performance-fix`, `export-pii-masking`, `live-assistant`). The compiler apparently summarises bug tables into prose ("some bugs were identified") and discards IDs.
4. **Role/title capture still missing**: `saurabh-sharma`, `vinay-kaushal`, `pratik-singh`, `sandeep-garg` — sources carry email-signature titles that aren't extracted. First-audit recommendation 6 not acted on.
5. **Source-file undercounting**: `sandeep-garg` is in 54 raw files, wiki cites 1. Same for `amit-agarwal` (first audit), `pratik-ahuja` (fixed from 0 → 3, still under actual count), and most people who appear as habitual CCs.
6. **Rendering bug persists**: `topics/crashagent.md` has a duplicated Team + Related block at EOF (same shape as `neeraj-agrawal.md` trailing-block bug from first audit).
7. **Near-duplicates verified distinct**: `vinay-kaushik` (vinay01@) vs `vinay-kaushal` (vinaykaushal@) — different emails, correctly separate. `tanya-agrawal` / `neeraj-agrawal` / `ravi-shekhar-agrawal` — distinct. No merges needed.
8. **Slug drift**: `sandeep-kaur1.md` uses email-prefix disambiguation — ugly but harmless; worth normalising to `sandeep-kaur.md` once.

## Delta from first audit

**Fixed**:
- `export-indiamart` / `tawk-to` byte-duplication: resolved.
- `systems/amarinder-s-dhaliwal` → `entities/amarinder-s-dhaliwal`: correctly re-typed.
- `entities/wazuh-mcp` → `systems/wazuh-mcp`: correctly re-typed.
- `pratik-ahuja` no longer a stub.

**Not fixed / regressed**:
- Stub-source-backfill: 3 of 4 previously-flagged stubs are still empty; 2 new stubs (amarinder, wazuh-mcp) were created instead.
- Bug/ticket ID capture: same drop-rate in new pages as in old.
- Entity role/title extraction: no improvement.
- Trailing-block duplication bug: repeats in `crashagent.md`.

## Recommendations (for compiler prompt, before next wave)

1. **Second-pass stub-filler** — after first compile, enumerate all pages with `sources: []` and re-run with the raw files that already wikilink back to them. Set a success gate: 0 stubs at commit.
2. **Preserve bug/ticket tables verbatim** — when a source contains a bug matrix (detect: markdown table with `Bug ID`/`Ticket`/`Work Package`/`ID` header), emit it as-is under a `## Bugs / Tickets` section. Never summarise into prose.
3. **Signature extraction** — parse the last 8 lines of each raw body for `Sr. Engineer`/`Manager`/`Lead`/`PM` title patterns and populate `role:` on the owning entity. Already a first-audit recommendation — unimplemented.
4. **Dead-wikilink lint** — post-compile, resolve every `[[slug]]`; fail build if unresolved (`[[buylead]]`). Would have caught 1 issue this pass.
5. **Trailing-block dedup** — compiler emits duplicate Team/Related blocks. Likely a chunking race — deduplicate identical adjacent H2 blocks before write.
6. **Full source-linking** — for entity pages, `sources:` must list every raw file whose To/From/CC/body contains the person's email. This is the same recommendation 1 from first audit; still not done.
7. **Snapshot counts** — first audit's concern about technical-depth loss (root cause, test counts) recurs in `trustpulse.md` (dropped 75% of source content) and `auditmate.md`. Consider mandating a minimum source-content preservation ratio or a verbatim `## Source Excerpts` section.

Report path: `/Users/amtagrwl/git/email-knowledge-base/docs/reviews/audit-pass2-20260412T211108Z.md`
