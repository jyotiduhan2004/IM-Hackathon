# Coherence Review — 2026-04-13T02:39Z

Scope: docs vs code drift across README, CLAUDE.md, CHANGELOG, BACKLOG, 8 issue docs, the first audit, `pyproject.toml`, `src/compile/prompts.py`, and `scripts/`.

---

## 1. Drift between docs and code

### README promises modules that don't exist

`README.md` "Project layout" (lines 186–245) advertises:

- `src/compile/relations.py` — "Supersession & conflict detection"
- `src/wiki/index.py`, `src/wiki/search.py`, `src/wiki/lint.py`
- `src/api/server.py`

None of these exist. `src/compile/` contains only `compiler.py` and `prompts.py`; `src/wiki/` and `src/api/` each hold a lone empty `__init__.py`. Supersession/conflict logic lives entirely inside the prompt in `prompts.py` (lines 120–135), not a dedicated module. Lint lives in `scripts/lint_wiki.py`, not `src/wiki/lint.py`.

### CLAUDE.md page-type taxonomy contradicts code + prompt

`CLAUDE.md` (lines 64–71) still lists only **five** page types: `topic, entity, policy, timeline, conflict` — with "entity" defined as "a person, team, **product, or system**". But `src/compile/prompts.py` (lines 15–20, 73–78) and `src/compile/compiler.py:81` have moved to **six** types with `system` split out as its own directory (`wiki/systems/`). The CHANGELOG `ae5f0e1` entry documents the split ("entities = humans, systems = products/platforms/URLs/mailing-lists"), and `wiki/systems/` has 21 live pages — but the agent schema doc hasn't caught up.

### CLAUDE.md commands reference a non-existent `make` target

`CLAUDE.md:54` advertises `make pipeline` and `make check`; the actual `Makefile` defines `pipeline` (line 35) and `check` (line 39) correctly — these are **fine**. But `CLAUDE.md:48` says `make setup` will "create .env"; the Makefile's setup uses `cp -n .env.example .env 2>/dev/null || true` which will silently succeed if `.env.example` is missing. Not broken, just weakly documented.

### README usage snippet doesn't mention the Makefile or snapshot safety

`README.md:71-86` shows `uv run python scripts/compile_all.py` directly. The actual `compile_all.py` auto-snapshots to `.snapshots/pre-compile-*` and runs `validate_wiki.py` after — a major safety feature (CHANGELOG entry "Frontmatter corruption…"). Neither the README nor CLAUDE.md mentions snapshots, `validate_wiki.py`, or `make snapshot-list`/`restore`.

### `docs/issues/07-phased-delivery.md` is out of date

Phase 0 checklist (line 7) still reads "IN PROGRESS". Per the commit history (`01b8e50`, `ae5f0e1`, `188fe64`) and the successful first audit, Phase 0 has shipped and iterated twice.

Phase 2 task "MkDocs Material site generation" is listed `⬜` (line 71), but commit `c82c43a` landed the MkDocs Material viewer with `make wiki`, `make wiki-build`, `make wiki-lan`, plus `mkdocs.yml`, `mkdocs_hooks.py`, and `mkdocs-material` / `mkdocs-roamlinks-plugin` already in `pyproject.toml` (lines 34-35).

---

## 2. Stale BACKLOG items (already done, not crossed out)

`docs/BACKLOG.md` still lists the following as open even though they shipped:

- **P0 #1 "Wikilink casing 50/50 mixed"** (lines 33-42) — fixed by `lint_wiki.py normalize_wikilinks()` + prompt hardening (CHANGELOG `235dc74`, `b95f7da`).
- **P0 #2 "Date hallucination"** (lines 44-49) — fixed by `stamp_page_compiled_at` tool + prompt rule "NEVER write `last_compiled` yourself" (CHANGELOG `ddd0c5a`; tool lives in `compiler.py:93-119`). BACKLOG marks it "Partially fixed this session — need to re-run to verify", but the separate "Quality: date hallucination" section (lines 195-208) still lists three fix options as future work.
- **P0 #3 "Non-person entities"** (lines 51-57) — `wiki/systems/` exists, prompt and compiler both enforce the split, 21 pages already live there.
- **P0 #5 "Index.md stale"** (lines 64-68) — `compile_all.py:142-143` now calls `update_wiki_index.invoke(...)` once after all batches.
- **"Quality: wikilink casing inconsistency"** section (lines 211-221) — duplicate of P0 #1, same status.
- **"Performance: parallelize compilation"** (lines 101-126) — `scripts/compile_parallel.py` shipped in commit `b6368d6` as "experimental"; BACKLOG doesn't reference the file.

Likely still valid but unverified: P0 #4 (orphan entity pages), P0 #6 (entity-identity-by-email), P0 #7 (batch stall timeout), all P1/Phase-1+ items.

---

## 3. Hidden gotchas (references to things that don't exist)

- **README architecture diagram** (lines 120-136) lists `compiler.py → prompts.py → relations.py`. `relations.py` is vaporware.
- **`README.md:277`** links to external OSS reference `lucasastorian/llmwiki` — not broken, but note: we don't use it, worth clarifying as "reference" vs "dependency".
- **`pyproject.toml:13`** declares `mail-parser>=3.15.0` but `uv.lock` pins `mail_parser 4.2.1`, and `src/ingest/parser.py` **does not import it at all** (grep confirms: no `mail_parser` / `mailparser` anywhere in `src/`). The parser works directly off `gmail.RawMessage`. Dead dependency.
- **CLAUDE.md:87** points to `scripts/ingest_backlog.py` — correct.
- **Issue doc `07-phased-delivery.md:33`** claims `uv run python scripts/lint_wiki.py` reports "clean" as a DoD; lint still flags the known issues from the first audit — not met.
- **No `.pre-commit-config.yaml`** despite several user-global instructions and the tone of the rest of the stack assuming one. Not referenced in email-knowledge-base docs, so fine; noting for parity.
- **No `.github/` directory** — README mentions `https://github.com/indiamart-ai/email-knowledge-base.git` (line 54). Repo hosting is on GitHub, but there are no workflows/CODEOWNERS/issue templates. No claim made that they exist, so not a contradiction — just a gap.

---

## 4. Orphan features — code/config without docs

- **`src/compile/compiler.py:335-355`** `_make_chat_model()` supports `LITELLM_BASE_URL` routing. `.env.example` should document this, but README/CLAUDE.md only mention "LiteLLM" abstractly (README:49, 177).
- **`scripts/watch_and_compile.py`** exists; nothing in docs references it. Is this a Phase 1 spike or dead code?
- **`src/budget.py`** + LiteLLM budget check (`compile_all.py:108-110`, `164-169`) shipped in `c1ac39d` — not mentioned in README, CLAUDE.md, or BACKLOG.
- **`scripts/validate_wiki.py`** and **`scripts/snapshot_wiki.py`** are used from `compile_all.py` and exposed via `make snapshot*` targets — zero prose documentation beyond the CHANGELOG entry.
- **`large_tool_results/`** at repo root, gitignored per `5a03e22` ("stray harness overflow dir") — worth a README.md-style gitignore comment to explain to future contributors.

### Docs without code

- **Phase 1 FastAPI webhook** (`docs/issues/08-phase1-live-ingestion.md`) — `src/api/` is empty. Expected for unshipped Phase 1, but README (line 217) suggests `server.py` exists.
- **"Langfuse tracing from day one"** is sold in README:180 and called "✅" in phased delivery (line 22); `get_langfuse_handler` is wired in `compiler.py:428-444`, but the conditional `if settings.langfuse_enabled` + `langfuse_public_key` + `langfuse_secret_key` in `config.py` means it's off unless keys are set. BACKLOG's "Observability" section (lines 339-364) leans toward "LiteLLM UI is sufficient; Langfuse later". Conflicting posture between README/phased-delivery and BACKLOG.

---

## 5. What's actually solid

- **`src/compile/prompts.py`** is the single source of truth for compiler behavior, and the hard rules (lines 137-150), page-type invariants (167-178), and Wikilink rules (85-110) read as if they were written after real failures — because they were (matches CHANGELOG entries 1:1).
- **CHANGELOG format** is disciplined and links commits. Keep it.
- **`audit-20260412T200427Z.md`** is concrete, sample-based, cites message IDs. Reuse its structure.
- **Makefile** matches what's in code — every target maps to a real script.
- **`.gitignore` + `raw/.gitkeep`** layout is clean; directory structure in README:186-245 matches reality for everything except the vaporware modules called out above.
- **`pyproject.toml`** ruff/mypy config is strict and consistent with `src/` usage.
- **Phase 0 DoD commands in `README.md:71-86`** do run end-to-end today (first audit proves it) — the DoD is met even though `07-phased-delivery.md` hasn't been ticked.

Report path: `/Users/amtagrwl/git/email-knowledge-base/docs/reviews/coherence-20260413T023902Z.md`
